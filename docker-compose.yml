services:
  task-map:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
    networks:
      - app-net

networks:
  app-net:
    external: true
