services:
  task-map:
    build: .
    container_name: task-map
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3010:3000"
    volumes:
      - ./data:/app/data
    networks:
      - app-net

networks:
  app-net:
    external: true
